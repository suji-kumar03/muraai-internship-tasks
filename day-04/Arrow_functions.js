//Functions->code reusable,block of code,perform a specific task when it is called
//function
function add(x, y) {
    console.log(`sum: ${x + y}`);
}
add(10, 20);
//arrow function
const sub = (x, y) => console.log(`Subtraction: ${x - y}`);
sub(20, 10);
const mult =(a,b) =>console.log(`Multiplication: ${a*b}`);
